{
  "requireCleanWorkingDir": false,
  "safeBump": false,
  "increment": "conventional:angular",
  "buildCommand":
    "sed -e 's/{{version}}/${version}/g' manifest.json > static/manifest.json && npm run build && zip -r yantan-v${version}.zip dist",
  "afterReleaseCommand":
    "rmraf yantan-v${version}.zip && echo Successfully released ${version} to ${dist.repo}.",
  "src": {
    "tagName": "v%s"
  },
  "github": {
    "release": true,
    "assets": "./yantan-*.zip"
  },
  "npm": {
    "publish": false
  }
}
